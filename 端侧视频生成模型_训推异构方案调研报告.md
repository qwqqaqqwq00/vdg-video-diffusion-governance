# 端侧视频生成模型：训推异构组合、跨设备修复与推理加速调研报告

> **调研日期**：2026-08-13
> **场景**：训练侧 = NVIDIA 数据中心集群（H100/H200/B200/GB300 NVL72）；端侧推理 = Apple Silicon(MPS) + 消费级 NVIDIA GPU + 工业边缘 NPU 三类全覆盖。
> **核心问题**：训练侧与推理侧设备差异较大时，如何①组合训推方案 ②修复跨设备问题 ③加速推理。
> **参考锚点**：README（AI 负载建模 / 性能-能耗仿真交付件）、NVIDIA Omniverse DSX Blueprint（AI 工厂数字孪生 / 能耗-热仿真）、以及此前 MPS 黑屏/NaN 修复实战经验（`MPS_BLACK_VIDEO_FIX.md`）。
>
> **本文定位**：顶层综合报告。四份子报告（训练侧 / 跨设备数值鲁棒 / 推理加速 / 端侧运行时）作为附录支撑，本文提炼其交叉洞察，不重复子报告细节。子报告清单见文末。

---

## 0. 执行摘要

当前开源视频生成已全面收敛到 **DiT + Flow Matching + 3D 时空 VAE** 范式（Wan 2.2、HunyuanVideo、CogVideoX、LTX-2、Open-Sora 2.0 等），训练在 NVIDIA 数据中心以 **bf16 为主、fp8/NVFP4 为新趋势**完成，产出的 checkpoint（safetensors）需部署到算力/精度/架构差异极大的端侧。本调研的核心结论：

1. **异构训推的本质矛盾是"精度悬崖"**。训练侧 bf16/fp8 在 HBM 上从容，但端侧 MPS 的 Metal 融合内核缺陷、NPU 的 INT8/INT4、消费卡的显存上限，把同一模型推向精度下限。**VAE 是硬下限**（fp8 VAE 即可见伪影），**AdaLN 调制的 `1+scale` 灾难性抵消**是最隐蔽的崩溃源——你的 MPS 黑屏修复正是这条悬崖上的典型实例，且 2026 年仍在复现（ComfyUI #15315：M4 Max 上 MiniMax H3 T2V 全黑+音频 NaN）。

2. **修复有成熟范式，可分层复用**。你的"三处 cast fp32"可提炼为**设备无关的数值鲁棒补丁模板**（调制/归一化/非线性/大序列归约/VAE 五类敏感 op 强制 fp32 中间计算），并与 2026 年量化研究的"boundary block 保高精度"、"高脆弱层集 bf16 保护"同源叠加。更彻底的做法是**训练侧根因消除**——对 AdaLN scale 加"远离精度悬崖"正则（未见专门论文，是可发表的研究点）。

3. **推理加速已形成可组合技术栈**。端到端最大杠杆是**步数蒸馏**（50→4 步，~25×），其次是**注意力优化**（SageAttention3 在 RTX 5090 上 5×、Sliding Tile Attention 让 HunyuanVideo 945s→268s），再叠加**跨步缓存**（TeaCache 1.4–4.4×，设备无关）、**VAE 分块**（32GB→8GB）、**量化**（GGUF/NVFP4/INT8）。可组合配方在消费 NV 上保守可达 **30–60×** 端到端加速。

4. **三类端侧能力分层明确**。消费级 NVIDIA 最成熟（ComfyUI 原生 + LightX2V，NVFP4 在 5090 上 >50×）；Apple Silicon 靠统一内存装大模型但带宽受限（M3 Ultra 819 GB/s），加速栈缺 SageAttention/FlashAttention，MLX 是更稳的替代后端；工业边缘 NPU 中 Jetson Thor 可行（128GB 统一内存 + 2070 TFLOPS FP4）但带宽仅 273 GB/s，国产服务器级卡（Ascend 910B 等）经 LightX2V/ComfyUI 适配是现实主路径，低端移动 NPU 不可行。

5. **可对接 README 的性能-能耗建模**。训练侧有公开成本锚点（Open-Sora 2.0：4160 H200·day / $199.6k）；端侧有功耗-延迟数据（4090 450W / 5090 575W / Thor 40–130W / Mac ≤480W）。DSX Blueprint 的 OpenUSD 数字孪生 + CFD 热仿真 + 电气负载范式，可直接用于"训推异构全链路能耗仿真"的建模交付件。

---

## 1. 异构训推的本质矛盾：精度悬崖

### 1.1 训练侧与端侧的精度鸿沟

| 维度 | 训练侧（NVIDIA DC） | 端侧（MPS / 消费NV / NPU） | 鸿沟 |
|---|---|---|---|
| **算力精度** | bf16 为主；Hopper/Blackwell fp8/NVFP4 训练 | MPS: bf16/fp16/分组量化；消费NV: fp8/fp4/int8；NPU: int8/int4 | 训练从容、端侧逼近下限 |
| **注意力后端** | FA-3（H100 740 TFLOPs）/ fp8 attention | MPS: 无融合 kernel（数学回退）；消费NV: SageAttention；NPU: 需原生移植 | MPS 注意力无加速且数值风险高 |
| **显存/内存** | HBM3e 80–192GB / 3.35–8 TB/s | MPS 统一内存 64–512GB / 410–819 GB/s；消费NV 24–32GB；NPU 64–128GB / 273 GB/s | 端侧容量够但带宽差 4–10× |
| **VAE 处理** | FP32/TF32 高精度 | 端侧常被迫量化或分块 | VAE 是精度硬下限 |
| **数值稳定性** | 大规模分布式，CUDA 内核成熟 | MPS 有系统性内核缺陷（持续到 2026）；NPU 算子覆盖缺口 | 端侧数值发散频发 |

### 1.2 精度悬崖的六个崩溃点（来自跨设备鲁棒报告）

训练 bf16/fp8 → 推理低精度时，六类 op 易发散，按危险度排序：

| 崩溃点 | 机理 | 阈值/条件 | 你的实战对应 |
|---|---|---|---|
| **AdaLN 调制 `(1+scale)`** ★最隐蔽 | bf16 7 位尾数灾难性抵消，`scale≈-1` 时 `(1+scale)→0` 丢全部有效数字 | `\|1+scale\|<2^-7≈0.0078` | 你已定位并修复（scale_msa/shift_msa/gate_msa + scale_mlp/...） |
| **GELU-tanh 融合内核** | MPS Metal 融合内核缺陷（非溢出）；fp16 则 `x³` 溢出 | bf16 `\|x\|≥15` NaN；fp16 `\|x\|>40` 溢出 | 你已定位并修复（cast fp32） |
| **Softmax/attention** | 大序列 `x-max` 减法 NaN/下溢 | seq≥万级 | PyTorch #96602（Open，2023 至今） |
| **RMSNorm/LayerNorm** | 平方和溢出/方差精度差 | fp16 `\|x\|>256` 平方溢出 | PyTorch #96113（MPS fp16 崩溃） |
| **VAE 解码** | 大数值范围、GroupNorm 不稳 | 中间特征 O(100) | 社区共识 VAE 不与 DiT 同步量化 |
| **时间步嵌入** | 频率展开量级跨度大→喂 AdaLN | timestep 静态固化 | 与 AdaLN 崩溃耦合（scale≈-1 的来源之一） |

**关键洞察**：这六个点不是独立的——**时间步嵌入的投影误差 → AdaLN 的 scale ≈ -1 → 灾难性抵消 → 整层清零 → 累积黑帧**，是一条完整的崩溃链。你的 MPS 修复恰好切断了这条链的最关键两环（GELU + AdaLN）。

### 1.3 为什么 T2V 比 I2V 更易崩溃

跨设备鲁棒报告发现一个重要规律（解释了 ComfyUI #15315"仅 T2V 全黑"现象）：**T2V 的文本条件经 timestep + text embedding 双重调制，调制参数动态范围更大、更易出现 `scale≈-1` 的极端步**；而 I2V/Ref2V 的图像条件路径调制分布更温和。这意味着**跨设备验证必须覆盖 T2V 路径**，不能只测 I2V。

---

## 2. 训推异构组合方案

### 2.1 训练侧选择如何传导到端侧（训练报告桥接结论）

训练侧的每一个决策都会沿 checkpoint 传导到异构端侧，这是组合方案的起点：

| 训练侧决策 | 对端侧的影响 | 组合策略 |
|---|---|---|
| **bf16 训练** | 保留量化余量，端侧再降到 fp8/int8/int4 稳健 | **面向异构端侧首选 bf16 训练** |
| **fp8/NVFP4 训练** | 逼近精度悬崖，端侧再量化易崩（xDiT 实测纯 FP8 注意力产生视觉伪影） | 端侧需"首尾高精度步"的步级精度调度；或退回 bf16 权重导出 |
| **VAE 精度** | VAE 是精度硬下限，fp8 VAE 即可见伪影 | **VAE 必须 FP32/TF32 训练并单独高精度导出**，端侧至少 bf16 |
| **MoE 架构（Wan 2.2）** | 端侧需专家路由+条件执行，NPU 工程复杂 | 面向端侧导出 **dense 变体**（如 Wan 2.2-TI2V-5B），MoE 仅留云端 |
| **高压缩 VAE（4×32×32）** | 端侧 token 少→更快，但重建质量略降 | 弱硬件端侧反而更友好（少 token），但接受质量折损 |
| **文本编码器选型** | T5-XXL(~5B) 对端侧是显存/算力主负担 | 训练侧产出门类更轻的 TE 或蒸馏对齐特征；端侧剪枝/量化/ CPU offload |

### 2.2 训练→端侧的导出分档矩阵

同一个训练 checkpoint，应按端侧能力导出多个变体（这是组合方案的核心工程动作）：

| 端侧类型 | 导出精度 | 注意力路径 | VAE 处理 | TE 处理 | 典型模型规模 |
|---|---|---|---|---|---|
| **消费 NV Blackwell（5090）** | NVFP4 / fp8 | SageAttention3（FP4）/ FA-4 | fp8 分块 | fp8 或量化 | 14B 舒适，720P |
| **消费 NV Ada（4090）** | fp8 / GGUF Q4 | SageAttention2（INT4 QK） | 时间分块 bf16 | GGUF/offload | 14B 需蒸馏+offload，480P |
| **Apple Silicon（MPS/MLX）** | bf16 + 分组量化 / GGUF Q4_K_M | MLX `mx.fast.scaled_dot_product_attention`（无 SageAttn） | bf16 分块 | GGUF/offload/MLX量化 | 1.3B 常态 / 14B 需大内存+蒸馏 |
| **Jetson Thor** | NVFP4 / INT8 | TensorRT（FP4） | INT8 分块 | INT8/offload | 14B NVFP4 4步蒸馏，短 clip |
| **国产服务器 NPU（Ascend 910B）** | INT8 / HiF8 | 厂商 SDK 原生移植 | INT8 分块 | INT8/offload | 14B INT8 经 LightX2V |
| **移动 NPU（RK3588 等）** | INT8/INT4 | 需自定义算子/回退 | 极端分块 | 必须蒸馏压缩 | **不可行**（仅极端蒸馏微型） |

### 2.3 训练框架与导出工具链

```
训练侧（NVIDIA DC）                      端侧
┌─────────────────────┐                ┌──────────────────────────┐
│ bf16 训练            │                │ Apple Silicon             │
│  Megatron-LM/NeMo    │──safetensors──→│  MLX 量化 / GGUF / CoreML │
│  DeepSpeed ZeRO+CP   │     bf16       │ 消费 NV                   │
│  ColossalAI(OpenSora)│                │  ComfyUI 原生 FP8 / TRT   │
│  FSDP(Wan)           │──safetensors──→│ NPU                       │
│                      │     fp8(可选)   │  LightX2V / torch_npu    │
│ 导出分档:            │                │  /厂商 SDK INT8           │
│  - bf16 主权重       │──GGUF Q4_K_M──→│  (city96 量化脚本)        │
│  - dense 变体        │                │                          │
│  - VAE 单独 FP32     │──CoreML/ONNX──→│                          │
│  - 蒸馏 4 步学生     │   (校准/转换)   │                          │
└─────────────────────┘                └──────────────────────────┘
```

**关键工程节点**：
- **Megatron Bridge**：训练用 Megatron、端侧用 HF/Diffusers 的 checkpoint 互转。
- **ComfyUI-GGUF**（city96）：DiT/transformer 对量化友好（线性层主导、卷积少），TE/VAE 分离处理。
- **LightX2V**（ModelTC）：已适配 Ascend 910B、寒武纪 MLU590、MetaX、摩尔线程等国产卡，是国产 NPU 跑视频 DiT 的现实主路径。
- **步数蒸馏**（CoDMD/Dynamic-in-Few-Step）：训练侧做、端侧只享用，是把 14B 大模型压到端侧可用的最关键杠杆。

---

## 3. 跨设备问题修复：从补丁模板到训练侧根因消除

### 3.1 通用数值鲁棒补丁模板（你的 MPS 修复的升华）

你的"MPS 三处 cast fp32"修复可提炼为**设备无关**的原则（跨设备鲁棒报告 §7）：

> **在低精度后端上，凡含"加性抵消风险（1+scale）、多项式非线性（GELU/SiLU）、归一化统计（Norm）、大序列归约（softmax）、大数值范围（VAE）"的 op，其敏感中间计算强制 fp32，仅边界张量按后端精度 cast。**

```python
# 设备能力探测
PREC_GUARD_OPS = {"gelu","gelu_tanh","silu","adln_modulate",
                  "rmsnorm","layernorm","groupnorm","softmax",
                  "timestep_embed_mlp","vae_decode"}
KEEP_FP32 = (device=="mps") or (backend in {"int8","int4","coreml_ane"})

def adln_modulate(x, scale, shift, gate=None):
    if KEEP_FP32:
        x32, s32, sh32 = x.float(), scale.float(), shift.float()
        y = rms_norm_fp32(x32) * (1.0 + s32) + sh32   # (1+scale) 在 fp32
        if gate is not None: y = y * gate.float()
        return y.to(x.dtype)
    return rms_norm(x) * (1 + scale) + shift
```

**各端侧落地**：
- **MPS**：你的三处 + 扩展 RMSNorm/softmax/timestep/VAE。
- **消费 NV (INT8)**：同模板 + SmoothQuant + per-channel/per-token + boundary block 保 bf16。
- **NPU（Ascend/Hexagon）**：HiF8/int8 + 首尾 block 保 bf16（Wan2.1-T2V-14B 在 Ascend 910B 的 boundary-protection 即此模板的 block 级实例）。
- **CoreML/ANE**：`compute_precision` 对敏感 op 指定 FLOAT32；attention 改原生 op（规避 coremltools #1763 flexible shape 失败）。
- **ONNX**：导出时对敏感 op 强制 fp32 节点；softmax 用 fp32 op；timestep 动态 axes。

### 3.2 三种保精度粒度（同源可叠加）

2026 年量化研究印证了你的思路，并提供了 block/层集级粒度：

| 粒度 | 方法 | 出处 | 与你的修复关系 |
|---|---|---|---|
| **op 级** | 敏感 op 强制 fp32 | 你的 MPS 修复 | 最细粒度 |
| **block 级** | 首尾 boundary block 保 bf16 | Wan2.1-T2V-14B HiFloat8（前2+后3 block） | 同原则粗粒度 |
| **层集级** | "高脆弱层集" bf16 保护 | Ideogram 4.0 INT8 | 介于两者之间 |

三者同一原则的不同粒度，可叠加使用。

### 3.3 训练侧根因消除（更彻底，含可发表研究点）

与其在推理侧打补丁，不如在训练侧消除发散根源。跨设备鲁棒报告提出五条训练侧策略：

1. **★AdaLN scale 远离精度悬崖正则**（可发表研究点）：在损失加 `λ·mean(relu(τ - |1+scale|))`（τ≈0.05），约束 scale 不进入 bf16 抵消区。**直接从训练侧消除灾难性抵消源，未见专门论文做此正则**，与推理期 boundary-block 保高精度互补。

2. **训练期模拟端侧混合精度（fake-quant QAT）**：前向插 `quant_dequant` 模拟目标端侧的"int8/int4 权重 + bf16 激活 + 关键 op fp32"，反向用 STE。用 EfficientDM 的 QALoRA + 蒸馏，或 QVGen（视频专用 QAT）。

3. **Hadamard 旋转预处理**：训练期固定旋转，让模型收敛到旋转友好分布（近高斯），下游 int8/int4/fp8 任何格式都更稳（OrbitQuant/HQ-DM/HyperQuant 实测"旋转后 int8 击败 fp8"）。

4. **蒸馏到量化友好的小模型**：与其量化 14B，不如蒸馏到 1–2B 学生再量化（SDXS + CoreML 达实时）。学生激活分布更窄、outlier 更少。

5. **fp8 训练需匹配端侧导出分档 + 重要性采样校正**：fp8 训练若未做 AIS 重要性采样校正，产出的权重带数值偏置，下游端侧更易发散。

### 3.4 跨设备验证方法

| 方法 | 用途 | 工具/出处 |
|---|---|---|
| **逐 op 数值探针**（你的第3节方法） | CPU fp32 vs 目标设备逐 op 对比，定位发散点 | PyTorch #96602 报告者已验证的范式 |
| **输出保真度对比** | PSNR/SSIM/LPIPS 对比量化输出与 fp32 reference | Ideogram 4.0 用 LPIPS 0.243 验证 |
| **黑帧/NaN 自动检测** | 每步 `isnan/isinf` 断言；帧均值/std≈0 即黑帧 | ComfyUI #15315 编码器侧自动捕获 |
| **回归测试集** | 固定 prompt/timestep/seed 网格，含"已知易触发"case | 必须**覆盖 T2V 路径**（§1.3） |
| **确定性基线** | CPU fp32 为基线（MPS backward 非确定性，#190632） | 记录 torch/驱动/macOS 版本 |

---

## 4. 推理加速技术栈：可组合配方

### 4.1 加速技术全景（按杠杆排序）

| 杠杆 | 代表技术 | 加速比 | 质量 | 训练/推理 | 适用设备 |
|---|---|---|---|---|---|
| **步数蒸馏** ★最大 | CoDMD（Wan 50→4步） | ~25× | VBench 84.5–84.9 | 训练侧 | 消费NV |
| | Dynamic-in-Few-Step（Wan-14B） | 30× | 定性 | 训练侧 | 消费NV |
| | WanToFight | 30 FPS@5090 | — | 训练侧 | 消费NV(5090) |
| **注意力优化** ★次大 | SageAttention3（FP4） | 5× over FA@5090 | FP4 精度较低 | 推理侧 | 消费NV(Blackwell) |
| | SageAttention2（INT4 QK） | 3× FA2 / 4.5× xformers | 可忽略 | 推理侧 | 消费NV(4090+) |
| | Sliding Tile Attention | 2.8–17× FA2；Hunyuan 945→268s | 0.09% VBench | 推理(+微调) | 消费NV |
| | 线性注意力(SANA-Video 2.0) | 16× vs Wan1.3B；单H100 120× vs Wan2.2 | VBench 84.30 | 训练侧 | 消费NV(5090)/H100 |
| **跨步缓存** ★设备无关 | TeaCache | 1.4–4.4× | −0.07% VBench | 推理侧 | **全设备** |
| **VAE 优化** | 时间分块 | 32GB→8GB(4×省显存) | 边界闪烁(overlap可控) | 推理侧 | 全设备 |
| | 流式 VAE(CogVideoX) | 19→11GB | 近无损 | 推理侧 | 全设备 |
| **量化** | NVFP4（Blackwell） | LightX2V >50×@5090 | 需QAT蒸馏 | 推理侧 | 消费NV(5090) |
| | GGUF Q4_K_M | Wan14B 29→10GB(fit) | 无正式VBench | 推理侧 | MPS/消费NV |
| | INT8(Jetson) | 9×延迟 | TRT拒transformer层 | 推理侧 | Jetson/NPU |
| **编译** | torch.compile | 1.5×(FLUX)；视频20–40% | 无回归 | 推理侧 | 消费NV/Jetson |
| | TensorRT FP8 | Firefly视频60%延迟/2.5×骨干 | — | 编译侧 | 消费NV/Jetson |
| | MLX mx.fast SDPA | 1.4–1.5× + 省4.3GB矩阵 | 位等价 | 推理侧 | MPS |

### 4.2 三个关键约束（组合时必须注意）

1. **MPS 无融合注意力**：FlashAttention/SageAttention 均仅 CUDA，MPS 上 SDPA 退化为非融合数学路径。Apple Silicon 注意力加速只能靠 **MLX `mx.fast.scaled_dot_product_attention`**（1.4–1.5×）或 **TeaCache**（跳过前向，设备无关）。这是 MPS 与消费 NV 加速能力的最大差距。

2. **蒸馏后注意力占比下降**：步数蒸馏把 50 步降到 4 步后，单步注意力仍是瓶颈但总步数少，SageAttention 的相对收益随之降低。组合加速不是简单相乘。

3. **消费卡与数据中心卡量化收益相反**：Ideogram 4.0 实测——A100/B200 原生 bf16/FP8 太快，INT8 fused kernel 反而落后；消费 Ampere（RTX 3090）无 FP8 tensor core，INT8 fused 才领先。**同一量化方案在消费卡与数据中心卡上收益相反**，端侧策略需独立选型。

### 4.3 可组合加速配方（端到端）

| 配方 | 适用设备 | 组合 | 估算端到端加速 |
|---|---|---|---|
| **配方1：蒸馏+缓存+量化注意力+VAE分块** | 消费NV(4090/5090) | CoDMD 4步 + TeaCache + SageAttention2 + VAE时间分块 | **30–60×** over 50步基线 |
| **配方2：滑窗注意力+FP4+编译** | 消费NV Blackwell(5090) | STA + SageAttention3 + torch.compile区域 + FP8 | **5–7×**（Hunyuan 945→~150–250s） |
| **配方3：GGUF+offload+分块+缓存** | 低显存消费NV + **MPS** | GGUF Q4 + block-swap + VAE时间分块 + TeaCache | ~2×（**核心价值是 fit：从跑不动到跑得动**） |
| **配方4：线性注意力+NVFP4+编译** | 消费NV(5090)/H100 | SANA-Video 2.0 + NVFP4 + 编译DiT | **~38×**（vs Wan1.3B） |
| **配方5：TRT FP8+编译+Graphs+INT8 VAE** | 消费NV + Jetson | TensorRT FP8 + torch.compile + CUDA Graphs + INT8 VAE | **~3.5×** |

> 注：组合估算为保守值，多技术叠加存在瓶颈转移与作用域重叠，非简单相乘。详见推理加速报告 SECTION B。

### 4.4 长视频生成的显存控制

长视频随帧数线性/二次增长显存，缓解靠四层：
1. **分块去噪**（Diffusion Forcing / Frame Pack）控制单步激活
2. **滚动/压缩 KV cache**（Self-Forcing / Forcing-KV）控制跨块累积
3. **分块 VAE**（HunyuanVideo 32GB→8GB）控制解码峰值
4. **上下文窗节点**（Kijai：1025帧→窗81+重叠16→<5GB@5090）控制端到端

---

## 5. 端侧选型决策树

```
端侧设备选型
  │
  ├─ 统一内存 ≥128GB（Mac M3 Ultra 512GB / Jetson Thor 128GB）
  │   └─ 可常驻 14B bf16/fp8 无需 offload
  │      └─ 带宽(273–819 GB/s)是瓶颈 → 必上 4步蒸馏 + TeaCache
  │         ├─ Mac：原型/低并发长任务（MLX 路径，注意 MPS 数值坑）
  │         └─ Thor：边缘离线/批处理短 clip（NVFP4 + INT8）
  │
  ├─ 24GB 独立显存（RTX 4090）
  │   └─ 蒸馏 14B + FP8/GGUF Q4 + offload 跑 Wan/Hunyuan 480P
  │      └─ 基线 ~13min/clip，4步蒸馏可到 ~1min；避免全精度 14B(OOM)
  │
  ├─ 32GB Blackwell（RTX 5090）
  │   └─ 优先 NVFP4 + SageAttention3 + 4步蒸馏（Wan2.2-NVFP4-Sparse >50×）
  │      └─ 720P 可达；FP4 走量化感知蒸馏而非朴素 PTQ
  │
  ├─ 国产服务器 NPU（Ascend 910B / 寒武纪 MLU590 / MetaX）
  │   └─ 经 LightX2V 或 ComfyUI(torch_npu/torch_mlu) 落地
  │      └─ INT8/HiF8 + 4步蒸馏 + boundary block 保 bf16
  │
  └─ 移动 NPU（RK3588 6TOPS / Hexagon / APU）
      └─ 视频 DiT 不可行；需极端 4步蒸馏 + INT8/INT4 + 大幅降分辨率
         └─ 算子覆盖缺口需自定义算子/回退
```

### 5.1 三类端侧对比总表

| 维度 | Apple Silicon | 消费级 NVIDIA | 工业边缘/NPU |
|---|---|---|---|
| **显存/内存** | 统一 64–512GB | 独立 24/32/48GB | Thor 128GB统一；RK3588≤32GB |
| **峰值算力** | M3 Ultra 819 GB/s(带宽主导) | 5090 3352 AI TOPS FP4 | Thor 2070 TFLOPS FP4；RK3588 6 TOPS |
| **量化精度** | fp16/bf16/fp8(MLX)/GGUF；**无FP4硬件** | FP4/NVFP4/FP8/INT8/GGUF | FP4/FP8/INT8(Thor)；INT8/INT4(非NV) |
| **视频生成可达性** | Wan2.1官方MLX跑通；Hunyuan/LTX无原生移植 | **最成熟**(ComfyUI+LightX2V全覆盖) | Thor可行无公开延迟；国产卡经LightX2V |
| **可达延迟(480p 81帧)** | M4 Max: 1.3B ~75min(50步)/数分钟(4步蒸馏) | 4090D ~13min(40步)/~1min(4步蒸馏) | Thor多分钟级(外推) |
| **主要瓶颈** | 带宽、MPS数值坑、ANE覆盖低 | 24GB上限、5090功耗 | 带宽(273GB/s)、功耗、算子覆盖 |
| **推荐模型规模** | 1.3B常态/14B仅大内存+蒸馏 | 14B(蒸馏+FP8/NVFP4) | Thor:14B NVFP4 4步蒸馏 |
| **功耗** | ≤480W系统 | 575/450/300W | 40–130W(Thor)/5–15W(RK3588) |

---

## 6. 对接 README：性能-能耗建模视角

README 要求交付"性能-能耗仿真核心逻辑、系统用例、架构设计、测试报告"，强调建模指导系统优化、可演进性。本调研的数据可直接喂养该建模交付件。

### 6.1 训练侧成本/能耗锚点

**Open-Sora 2.0（11B，H200，ColossalAI）——唯一公开完整成本拆解**：

| 阶段 | GPU·day | 成本(@$2/GPU·h) |
|---|---|---|
| 256px T2V | 2240 | $107.5k |
| 256px T/I2V | 384 | $18.4k |
| 768px T/I2V | 1536 | $73.7k |
| **合计** | **4160** | **$199.6k** |

- 折算 ≈ 99,840 H200·hour；较 MovieGen/Step-Video 便宜 5–10×。
- **GB300 能效**：较 Hopper 5× TPS/MW、50× AI 工厂产出 → 每 token 能耗大幅下降。
- **高分辨率是成本大头**：768p 训练比 256p 慢 40× → 策略"低分辨率学运动 + 少量高分辨率微调"。

### 6.2 端侧功耗-延迟数据点（建模输入）

| 设备 | 功耗 | 延迟(480p 81帧) | 能效比 |
|---|---|---|---|
| RTX 5090 | 575W | 4步蒸馏~1min；NVFP4 >50× | 高（FP4 tensor core） |
| RTX 4090 | 450W | 40步~13min；4步蒸馏~1min | 中 |
| H100×8 | 数据中心 | 40步~30s | 高（集群） |
| M4 Max | ≤480W系统 | 1.3B 50步~75min；4步蒸馏~6min | 低（带宽受限） |
| Jetson Thor | 40–130W | 多分钟级(外推) | 中（FP4但带宽受限） |
| RK3588 | 5–15W | 不可行 | — |

### 6.3 DSX Blueprint 作为工业级能耗建模范式

NVIDIA Omniverse DSX Blueprint 用 OpenUSD 数字孪生 + CFD 热仿真 + 电气负载仿真，对 GB200/GB300 NVL72 机柜做设计/仿真/运营。其架构可**直接迁移到训推异构全链路能耗建模**：

| DSX 能力 | 训推异构建模映射 |
|---|---|
| **CFD hot-aisle 热仿真** | 数据中心训练集群的热-功耗仿真（GB300 液冷机柜级） |
| **电气负载仿真** | 训练+推理全链路能耗曲线（H200·hour → 端侧 W·clip） |
| **机柜级配置（GB200/GB300 NVL72）** | 训练集群规模-能效权衡（TPS/MW、GPU·day/成本） |
| **WebRTC 流式渲染 + AI agent** | 端侧推理服务的实时监控/数字孪生可视化 |
| **OpenUSD 场景结构** | 训推异构全链路的统一场景描述（训练集群→导出→端侧部署） |

**建模可演进性**（README 要求）：DSX 的 OpenUSD 层使"训练集群数字孪生"可逐步扩展到"端侧部署数字孪生"——把端侧设备的功耗/延迟/精度约束作为 SimReady 资产接入，实现"训推全链路性能-能耗联合仿真"。这正契合 README 的"抽象能力/可演进性/可扩展性"评分维度。

### 6.4 性能-能耗权衡策略（README 核心要求）

基于本调研数据，可提炼以下权衡结论供建模交付件使用：

1. **训练侧：bf16 + 低分辨率学运动 + 高分辨率微调**——成本/能耗最优（Open-Sora 2.0 验证 5–10× 成本优势）。
2. **端侧：步数蒸馏是能效第一杠杆**——50→4 步不仅省时间，更直接降能耗（4 步 vs 50 步 = ~12× 能耗降），且蒸馏在训练侧一次性完成。
3. **端侧量化按设备分档**——Blackwell 用 NVFP4（能效最高）、消费 Ada 用 INT8 fused（无 FP4）、MPS/NPU 用 INT8 + 关键 op 保 fp32（能效与精度平衡）。
4. **长视频靠分块而非加卡**——分块去噪 + 分块 VAE + 滚动 KV 控制显存，避免为长视频堆硬件（能耗/成本更优）。

---

## 7. 结论与可演进性

### 7.1 三大核心诉求的回答

**①如何组合训推方案**：
训练侧 bf16 训练（留量化余量）→ VAE 单独 FP32 导出 → 按端侧能力分档导出（NVFP4/INT8/GGUF/dense变体）→ 蒸馏 4 步学生 → 端侧用 LightX2V/ComfyUI/MLX 部署。MoE 仅留云端，端侧用 dense 变体。

**②如何修复跨设备问题**：
推理侧用通用数值鲁棒补丁模板（五类敏感 op 保 fp32，op/block/层集三粒度可叠加）；训练侧根因消除（AdaLN scale 远离精度悬崖正则 + QAT 模拟端侧精度 + Hadamard 旋转）；验证用逐 op 探针 + PSNR/SSIM/LPIPS + 黑帧检测，必须覆盖 T2V 路径。

**③如何加速推理**：
端到端配方 = 步数蒸馏(25×) + 注意力优化(SageAttn/STA, 2–5×) + 跨步缓存(TeaCache, 1.4–4.4×, 设备无关) + VAE分块(4×省显存) + 量化(NVFP4/GGUF)。消费 NV 保守 30–60×；MPS 受限（无融合注意力，靠 MLX+TeaCache）；NPU 靠 INT8 + 蒸馏。

### 7.2 可演进性（README 评分维度）

- **模型层可演进**：DiT + Flow Matching 范式稳定，MoE/高压缩 VAE/线性注意力(SANA-Video)是演进方向。
- **加速技术可组合**：蒸馏/缓存/注意力/量化/编译五族正交，可按设备组合。
- **修复模板可复用**：op/block/层集三粒度保精度，设备无关。
- **建模可扩展**：DSX OpenUSD 数字孪生可从训练集群扩展到端侧全链路。
- **训练侧研究点**：AdaLN scale 正则、QAT 模拟端侧精度、Hadamard 旋转训练——均有 2026 论文支撑但尚未成熟，是可深入的"扎到根"方向。

### 7.3 风险与缺口

- **MPS 生态缺口**：无 FlashAttention/SageAttention，MLX 视频刚起步（仅 Wan2.1 官方移植），CoreML 无原生视频 DiT。
- **NPU 算子覆盖**：3D 注意力/AdaLN/RoPE 在非 NV NPU 上支持差，需自定义算子。
- **fp8 训练权重**：逼近精度悬崖，端侧再量化易崩，需谨慎。
- **公开数据缺口**：Wan/HunyuanVideo 训练成本未公开；Jetson Thor 视频生成无公开延迟；ComfyUI-GGUF 质量无正式 VBench。

---

## 附录：子报告清单

本综合报告基于四份子报告（均为 2026-08-13 调研，web_fetch 抓取一手来源）：

| # | 子报告 | 文件 | 重点 |
|---|---|---|---|
| 1 | 训练侧 NVIDIA DC 方案 | `video-gen-training-nvidia-2025-2026.md` | 模型架构/分布式策略/混合精度/成本能耗/导出 |
| 2 | 跨设备数值鲁棒性 | `DiT_跨设备数值鲁棒性研究报告.md` | 发散根因/转换管线/修复模板/QAT/验证 |
| 3 | 推理加速技术栈 | `video-dit-inference-acceleration-report.md` | 蒸馏/缓存/注意力/VAE/量化/编译/ComfyUI/配方 |
| 4 | 端侧三类运行时栈 | `edge-video-dit-deployment-2026.md` | MPS/MLX/消费NV/Jetson/NPU 规格与落地 |

> **方法论说明**：四路调研环境的 `web_search`(Brave) API key 均未配置，故全程改用 `web_fetch` 直连 arXiv API、GitHub raw README、HuggingFace 文档、NVIDIA/Apple/PyTorch 官方页面等一方来源。凡引用 arXiv 论文均给出真实 ID；凡引用 issue 均给出真实编号与状态；未检索到数据处一律标注"未报/未检索到"，不编造。如需恢复通用网络检索（基准博客、ComfyUI 论坛、厂商文档），请运行 `/web-tools` 配置 Brave key。

---

## 参考来源（核心）

### 模型与训练
- Wan 2.2：https://github.com/Wan-Video/Wan2.2 · Wan 2.1 技术报告 https://arxiv.org/abs/2503.20314
- HunyuanVideo：https://huggingface.co/tencent/HunyuanVideo · https://arxiv.org/abs/2412.03603
- CogVideoX：https://github.com/THUDM/CogVideo · https://arxiv.org/abs/2408.06072
- Open-Sora 2.0（成本拆解）：https://arxiv.org/abs/2503.09642
- xDiT（USP/PipeFusion）：https://github.com/xdit-project/xDiT
- Megatron-LM：https://github.com/NVIDIA/Megatron-LM
- TransformerEngine（fp8 收敛验证）：https://github.com/NVIDIA/TransformerEngine
- GB300 NVL72（能效 5× TPS/MW）：https://www.nvidia.com/en-us/data-center/gb300-nvl72/

### 跨设备数值鲁棒
- ComfyUI #15315（M4 Max MiniMax H3 T2V 全黑）：https://github.com/Comfy-Org/ComfyUI/issues/15315
- PyTorch MPS 缺陷：#96602(softmax NaN,Open) / #96113(LayerNorm崩溃) / #190632(非确定性梯度)
- Wan2.1-T2V-14B HiF8 boundary-protection（Ascend 910B）：https://arxiv.org/abs/2606.00957
- Ideogram 4.0 INT8 + bf16 高脆弱层保护：https://arxiv.org/abs/2606.12280
- 消费 INT8 陷阱（Ideogram 4.0）：https://arxiv.org/abs/2606.14598
- HyperQuant（LTX-2 19B 近无损）：https://arxiv.org/abs/2606.23406
- QVGen（视频 QAT）：https://arxiv.org/abs/2505.11497
- MLX vs MPS：https://arxiv.org/abs/2511.05502
- ComfyUI-GGUF（DiT 量化友好）：https://github.com/city96/ComfyUI-GGUF

### 推理加速
- TeaCache（CVPR 2025）：https://arxiv.org/abs/2411.19108 · https://github.com/ali-vilab/TeaCache
- SageAttention 1/2/2++/3：https://github.com/thu-ml/SageAttention
- Sliding Tile Attention（Hunyuan 945→268s）：https://arxiv.org/abs/2502.04507
- SANA-Video 2.0（线性注意力 120×）：https://arxiv.org/abs/2607.21553
- Adobe Firefly 视频 TensorRT：https://developer.nvidia.com/blog/optimizing-transformer-based-diffusion-models-for-video-generation-with-nvidia-tensorrt/
- torch.compile + Diffusers：https://docs.pytorch.org/devlogs/inductor/2026-05-11-torch-compile-and-diffusers/

### 端侧部署
- MLX Wan2.1（M4 Max 延迟）：https://github.com/ml-explore/mlx-examples/blob/main/video/wan2.1/README.md
- LightX2V（4090D/H100 benchmark + 国产 NPU 适配）：https://github.com/ModelTC/LightX2V
- RTX 5090（NVFP4 3352 TOPS）：https://www.nvidia.com/en-us/geforce/graphics-cards/50-series/rtx-5090/
- Jetson Thor（2070 TFLOPS FP4 / 128GB）：https://www.nvidia.com/en-us/autonomous-machines/embedded-systems/jetson-thor/
- ComfyUI 量化文档：https://github.com/Comfy-Org/ComfyUI/blob/master/QUANTIZATION.md

### DSX Blueprint（能耗建模范式）
- DSX Blueprint for AI Factories：https://github.com/NVIDIA-Omniverse-blueprints/omniverse-dsx-blueprint-for-ai-factories
- DSX ARCHITECTURE.md（OpenUSD 场景结构 / CFD 热仿真 / 电气负载）
